name: Run Format Script

on: [push]

jobs:
  run-format-script:
    name: Run Format Script
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci

      - run: npm run format
